<?php
$order = Mage::getModel('sales/order')->load($this->getOrder()->getId());
$crmId = $order->getTimChanceId();
if ($crmId) {
    echo $crmId;
} else {
    echo $this->getOrder()->getIncrementId();
}
?>